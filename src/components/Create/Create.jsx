import React from "react";
import { Container } from "../generals/Container";

const Create = ({children}) => {
    return (
        <Container>
            {children}
        </Container>
    )
}

export {Create}